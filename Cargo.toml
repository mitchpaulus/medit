[package]
name = "medit"
version = "0.1.0"
edition = "2024"

[dependencies]
notify = "8"
regex = "1"
serde_json = "1"
tree-sitter = "0.25"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[build-dependencies]
cc = "1"
