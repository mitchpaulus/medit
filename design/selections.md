For selections/text motions, I prefer the 'repeat' approach over precise, one-off selections.

So instead of `ci"` and `ci(` for selections inside brackets, I would prefer to just mash `ALT-i` enough times to get where I needed.
